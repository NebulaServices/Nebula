/** @import { Config } from "aero-sandbox" */

import { defaultConfig } from "./defaultConfig.js";

/**
 * @type {Config}
 */
const config = {
	...defaultConfig,
	prefix: "/~/aero/"
};

export default config;
